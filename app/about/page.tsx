import Link from "next/link";
import { BrainCircuit, Database, FileCheck2, ShieldCheck } from "lucide-react";

export const metadata = { title: "About" };
export default function AboutPage() {
  return <div><section className="bg-slate-950 px-4 py-20 text-white sm:px-6"><div className="mx-auto max-w-5xl"><p className="font-bold uppercase tracking-widest text-teal-300">About GrantPilot</p><h1 className="mt-4 max-w-4xl text-5xl font-black leading-tight">Scholarship preparation built around evidence, tools, and transparent AI.</h1><p className="mt-6 max-w-3xl text-lg leading-8 text-slate-300">GrantPilot is a production-oriented full-stack application that demonstrates secure engineering and practical agent workflows without presenting model output as an official decision.</p></div></section><section className="mx-auto max-w-7xl px-4 py-20 sm:px-6 lg:px-8"><div className="grid gap-6 md:grid-cols-2 lg:grid-cols-4">{[[Database, "Sourced records", "Listings store official and source URLs, verification dates, and deadline uncertainty."], [BrainCircuit, "Reasoning workflows", "Agents retrieve context, call specialised tools, validate outputs, and persist useful results."], [FileCheck2, "Document evidence", "Uploaded files become structured reports with key points, risks, gaps, and actions."], [ShieldCheck, "Secure ownership", "JWT cookies, role checks, validation, rate limits, and user-scoped database queries protect records."]].map(([Icon, title, text]) => { const C = Icon as typeof Database; return <div key={String(title)} className="rounded-2xl border bg-white p-6"><C className="text-teal-600" size={30} /><h2 className="mt-6 text-xl font-black">{String(title)}</h2><p className="mt-3 leading-7 text-slate-600">{String(text)}</p></div>; })}</div><div className="mt-16 grid gap-12 lg:grid-cols-2"><div><h2 className="text-3xl font-black">What the platform does</h2><p className="mt-5 leading-8 text-slate-600">It combines scholarship discovery, profile-aware recommendation, document intelligence, saved-item tracking, deadline dashboards, and conversational assistance. Every major feature is backed by API routes and MongoDB collections rather than hard-coded interface state.</p></div><div><h2 className="text-3xl font-black">What the platform does not do</h2><p className="mt-5 leading-8 text-slate-600">It does not guarantee funding, replace official eligibility checks, submit applications on a user&apos;s behalf, or claim that an estimated deadline is confirmed. Users are directed to authoritative programme sources.</p></div></div><div className="mt-16 rounded-[2rem] bg-teal-600 p-8 sm:p-12"><h2 className="text-3xl font-black">Ready to build your evidence profile?</h2><p className="mt-3 max-w-2xl leading-7">Create an account, complete factual academic details, and run the recommendation workflow.</p><Link href="/register" className="mt-7 inline-flex rounded-xl bg-slate-950 px-6 py-3 font-black text-white">Get started</Link></div></section></div>;
}
