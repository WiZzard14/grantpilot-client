export const metadata = { title: "Privacy Policy" };
export default function PrivacyPage() { return <article className="mx-auto max-w-4xl px-4 py-16 sm:px-6"><p className="font-bold uppercase tracking-widest text-teal-700">Legal</p><h1 className="mt-3 text-5xl font-black">Privacy policy</h1><p className="mt-4 text-slate-500">Demonstration policy · Updated July 17, 2026</p><div className="mt-10 grid gap-9 text-slate-700">{[["Information stored", "The application stores account details, academic profile fields, saved scholarship states, interaction events, uploaded document metadata, extracted text, AI reports, and conversations needed to provide the selected features."], ["Uploaded documents", "Documents are scoped to the authenticated user. File type and size are validated. In a production deployment, object storage encryption, retention rules, malware scanning, and scheduled deletion should be configured."], ["AI processing", "When an external AI provider is configured, relevant prompt context or document content may be sent to that provider. The deterministic fallback does not send content to an external model."], ["Authentication", "Access and refresh tokens are placed in HttpOnly cookies. Passwords are hashed and never returned by the API."], ["Control and deletion", "Users can delete uploaded documents, conversations, saved scholarships, and submitted listings through available interfaces. Account deletion can be added to the same authenticated pattern."], ["Contact", "Privacy questions can be submitted through the contact page."]].map(([title, body]) => <section key={title}><h2 className="text-2xl font-black text-slate-950">{title}</h2><p className="mt-3 leading-8">{body}</p></section>)}</div></article>; }
